import { Meta, StoryObj } from "@storybook/react";

import Empty from ".";

export default {
  title: "components/Empty",
  component: Empty,
  parameters: {
    layout: "fullscreen",
  },
} as Meta;

export const Default: StoryObj = {};
